# App identifier matches capacitor / Codemagic
app_identifier("com.lilnetro0.nexus")

# Human metadata source (edit docs/store/en-US/listing.txt, then split into
# deliver folders or paste into ASC). deliver's default metadata_path is ./metadata —
# copy/split listing fields there when automating.
metadata_path("./metadata")

skip_binary_upload(true)
skip_screenshots(true)
force(true)

# Prefer ASC API key in CI:
# api_key_path ENV["APP_STORE_CONNECT_API_KEY_PATH"]
